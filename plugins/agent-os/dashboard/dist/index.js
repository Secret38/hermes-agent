// Agent OS Desktop UI is provided by the bundled Hermes Desktop plugin.
// This dashboard asset intentionally remains a no-op; the manifest's API is
// shared with Desktop and can also be consumed by the browser dashboard later.
export {};
